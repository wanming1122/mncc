lru.py 的 lru_cache 装饰器有两处行为错误：

1. 传入 list 这类不可哈希参数时直接抛 TypeError。期望：参数会先做规范化，等值参数（如 [1, 2] 与 (1, 2)）应命中同一个缓存项
2. 不同函数各自被 lru_cache 装饰后，缓存互相"串味"——对 double(5) 与 triple(5) 的调用可能返回彼此的结果。每个被装饰的函数必须拥有独立缓存

其余要求保持不变：按 LRU（最久未使用先淘汰）策略、maxsize 生效、缓存命中不重复执行函数。修复后运行 python 命令验证。