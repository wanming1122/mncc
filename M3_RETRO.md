# M3 复盘：工具集补全 + diff 预览 + 路径/命令守卫

> 写作日期：2026-09-03。事实来源：`M3_DESIGN.md`、`git log`（M1–M5 压缩于 `2e305aa`）、
> 代码与 `tests/`、README/INTERVIEW 最终版。文档未记录之处已标注「（分析）」「（对话记录）」。
> 「对话记录」指开发时的一手对话过程，未落盘于 git/文档，不可独立核验；以其为唯一依据的表述视同（分析）。

## 1. 目标与范围

工具面从 M2 的三个（`read_file`/`write_file`/`run_command`）补到六个：新增 `edit_file`、
`list_dir`、`grep`，配套三件横切能力：修改前 diff 确认、路径守卫、命令守卫。明确不做：
检查点回滚（§5.C 差异化模块，路线图后续列入 M7）、上下文压缩（M4）、黑名单配置化、
`run_command` 路径守卫——shell 命令无法被路径静态分析可靠沙箱化（`python -c "os.remove(...)"`
可写任意路径），与其给半套虚假安全感，不如诚实声明边界（`M3_DESIGN.md` 边界说明）。

## 2. 开发过程

顺序：**盘 M2 遗留 → 设计拍板 → 叶子模块 → 契约改造 → 新工具 → 装配 → 测试 → 验收脚本**。

1. 会话开头检查 M2 时发现 `true` 命令跨平台失效（Windows shell 无此命令），改为
   `python -c "pass"`——项目第一次踩跨平台坑（M6 CI 又抓了同族问题）。
2. 设计 6 条决策落 `M3_DESIGN.md`，黑名单语义、授权粒度两项经用户拍板（AskUserQuestion）。
3. 先写 `safety/guard.py`（零项目内依赖的叶子模块），再改 `tools/base.py` 契约，后上三工具，
   最后 `render/cli` 装配。**为什么这个顺序**：安全是横切面，晚补会漏入口；M2 的
   `preview()` 是 `json.dumps` 永不抛错，而 M3 的 edit 预览要读文件算 diff——不先在 base
   打补丁（D7），第一个 edit 调用就炸穿 agent 循环。
4. 测试跟随第 3、4 步（叶子模块、契约改造）推进，随新工具同步扩展 `test_safety.py`、
   `test_tools_search.py`，每步跑 `pytest + ruff` 及时暴露契约破坏。
5. 验收因"交互 REPL 不便 + 流程繁琐"改为脚本化：产出 `verify_m3.py`（无 API 跑两层，
   有 key 才跑端到端；M4 后追加压缩冒烟层沿用至 M6）。

## 3. 关键设计决策

| 决策 | 内容 | 被放弃的替代方案 |
|---|---|---|
| D1 黑名单语义 | `rm -rf`/`del /s`/`mkfs`/`format 盘符`/`curl\|sh`/`dd 写 /dev/`/fork 炸弹**绝对拦截，`--yolo` 不解锁** | 高危命令走 y/n 确认（放弃：`--yolo` 下可执行 `rm -rf`，红线变"确认两次"） |
| D2 授权粒度 | 记住精确命令串；批准 `python -m pytest` 不放行 `python -c "os.remove(...)"` | 按程序名首 token 记忆（放弃：授权恐慌放大） |
| D3 行尾保留 | 读 bytes 判主导行尾 → 按 LF 匹配 → 写回还原 | 文本模式读写（放弃：CRLF 文件一编辑全变行尾，diff 全红） |
| D4 未命中诊断 | 0 次命中先查行尾空白差异（**只提示不改**），再给相近片段 + mini diff | 自动归一空白替换（放弃：掩盖模型错误，评测失真） |
| D6 异常区分 （对应 M3_DESIGN|决策 6「修复 M2 隐患（D7）」）  `Sa段yViolation` 独立于 `ToolError`，safety 零依赖 | 并入 ToolError（放弃：registry 兜底包出"工具执行异常：SafetyViolation…"） |
| D7 确认段防护 | 确认回调算 preview 抛错 → is_error 回填，不炸循环（M2 隐患） | 维持 M2 现状（放弃：edit 预览必抛错） |

共性：每个决策都在"安全强度 vs 模型自主性"间取点——错误回填让模型自纠，红线硬停；
`is_error`（可继续）与 `ConfirmRefused`（须中止）的区分是这条线的实现（`tools/base.py`）。

## 4. 难点与踩坑

`M3_DESIGN.md` 未设难点章节，以下按代码痕迹与开发记录还原（已标注）：

- **守卫检查顺序**（对话记录；`safety/guard.py` 注释可查证）：曾把已授权集合判断放黑名单
  之前，导致批准过的 `rm -rf` 反而放行，恰好构成 D1 漏洞。防复发：测试用全量黑名单 cases
  钉死 `approve` 后仍 block。
- **py310 f-string 反斜杠**（分析；`pyproject` target py310）：diff 预览里
  `'\r\n'` 写进 f-string 表达式非法，现代码提取 `eol_label` 变量。防复发：先赋值再插值。
- **测试断言自身错误**（对话记录）：单层 `../` 本不越界却当穿越写；空白诊断用例的
  `old_string` 被诊断文本子串包含、绕过提示；preview 抛错用例没模拟真实确认回调。
  防复发：写守卫用例先脑内过一遍 `path.resolve()`，断言用保留字匹配。
- **M2 遗留 `true` 跨平台**（对话记录）：教训是"Linux 过、Windows 挂"的早期信号，M6
  `e6f67a6` 是同一主题二次爆发。

## 5. 验收与数据

- 测试：M2 末 112 项（`M2_DESIGN.md`）→ **M3 末 200 项收集，198 通过 + 2 符号链接用例
  跳过**（Windows 无建链权限），ruff 零报错（`M3_DESIGN.md` 状态行）。
- 装配验证：六工具齐全；`rm -rf` 经 registry 返回 is_error（`--yolo` 不解锁）；工作区外
  路径被拒；edit diff 预览含 `+/-`。
- `verify_m3.py` 实测第 1/2 层全绿。

## 6. 一分钟面试讲述版

M3 我给 agent 补全了工具面并立起安全边界。一是 `edit_file` 的唯一性契约：精确串替换
只允许命中一次，0 次给诊断（先查行尾空白差异、再给相近片段加 mini diff），多次列行号，
把"改坏代码"变成可自纠的错误。二是行尾保留：按主导行尾匹配再还原，否则 CRLF 文件
diff 全红。三是命令守卫：黑名单绝对拦截，`--yolo` 只跳过确认、不跳过红线；其他命令
首次确认，授权只记精确命令串，批准 pytest 不连带放行任意 python。四是路径守卫用
`resolve()` 展开 `../` 和符号链接再判界。安全模块零项目内依赖，可独立审计。测试从
112 增到 200 项，并写了一个无 API 也能跑的一键验收脚本。

## 7. 延伸与建议

- **重做会改什么**（分析）：黑名单是启发式，能拦"长成危险样子的命令"而非真正边界，
  `--yolo` 语义应做成 CLI 文档化契约；`PathGuard` 有 TOCTOU——`resolve()` 判界与 `open()`
  之间符号链接可被替换，硬化方向是 `O_NOFOLLOW` + 固定 fd。
- **面试追问**："黑名单怎么绕过？"→ 承认是防呆不是边界，执行级恶意需进程沙箱；
  "为什么不用行号/正则做 edit？"→ 行号会因前序修改漂移，正则隐式匹配不可预期；
  "`ConfirmRefused` vs `is_error`？"→ 前者是策略性失败必须硬停，后者是模型可自纠该回填。
- **后续呼应**：M6 `e6f67a6` 证明跨平台教训未一次根治，若重来 M3 就该给 `run_command`/
  `grep` 写平台无关断言；M7 检查点回滚正补上"agent 改坏代码"兜底，是 M3 安全面的自然延伸。